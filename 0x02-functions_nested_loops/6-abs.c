#include "main.h"
/**
 * _abs - convert number to absolte
 *@c: The int to convert to absolte
 * Return: absolute value
 */

int _abs(int c)
{
if (c < 0)
{
int _absVal = c + (c * -2);
return (absVal);
}

else
return (c);

}
